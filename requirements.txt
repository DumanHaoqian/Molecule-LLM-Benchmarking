# Dependencies are split across two virtualenvs (see scripts/).
#
# Generation venv (chemdfm) — model runtime:
#   torch>=2.7  transformers>=5.0  datasets>=3.0  rdkit>=2024.03
#
# Evaluation venv (ChEBI-20-Eva) — metrics, built by scripts/setup_eval_env.sh:
torch            # CPU build
numpy
scipy<1.14       # fcd_torch uses linalg.sqrtm(disp=...)
rdkit>=2024.03
nltk>=3.9
rouge_score>=0.1.2
fcd_torch
gensim           # Text2Mol mol2vec vectors
transformers     # Text2Mol SciBERT
huggingface_hub  # pinned ChemCoTBench snapshot downloads
pandas           # ChemCoTBench evaluator utilities
scikit-learn     # ChemCoTBench reporting metrics
